import { ProblemMetadata } from "./problem";
import { metadata as skyscrapersMetadata } from "./skyscrapers";
import { metadata as sudokuMetadata } from "./sudoku";

export const metadatas: ProblemMetadata[] = [skyscrapersMetadata, sudokuMetadata];
